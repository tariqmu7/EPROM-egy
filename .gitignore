# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Environment variables (contain Firebase credentials — never commit)
.env
.env.local
.env.development
.env.production
.env.*.local
!.env.example

# Firebase credentials — use the VITE_FIREBASE_* env vars instead
firebase-applet-config.json

# Generated from firestore.rules.template by `npm run rules:build`
# (bakes in VITE_BOOTSTRAP_ADMIN_EMAIL). Edit the template, not this file.
firestore.rules

# Reporting artifacts — generated reports/decks, not part of the app source
reporting/
skills/
job_profiles/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?
